---
phase: 04-classification-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classifier.py
  - main.py
autonomous: true
requirements: [CLF-01, CLF-02, CLF-03, CLF-04, OUT-01, OUT-02]

must_haves:
  truths:
    - "控制台打印 AUC、准确率、敏感性、特异性和置换检验 p 值"
    - "results.json 被创建，包含所有指标和配置信息"
    - "交叉验证按 subject 分组，无数据泄露（StratifiedGroupKFold）"
    - "1000 次置换检验完成并报告 p 值"
  artifacts:
    - path: "classifier.py"
      provides: "classify(X, y, groups) -> dict"
      exports: ["classify"]
    - path: "main.py"
      provides: "调用 classify()，打印结果，保存 results.json"
      contains: "results.json"
  key_links:
    - from: "main.py"
      to: "classifier.classify"
      via: "from classifier import classify"
      pattern: "classify\(X, y, groups\)"
    - from: "classifier.py"
      to: "StratifiedGroupKFold"
      via: "sklearn.model_selection"
      pattern: "StratifiedGroupKFold"
    - from: "main.py"
      to: "results.json"
      via: "json.dump"
      pattern: "json\.dump"
---

<objective>
用 SVM(RBF) + StratifiedGroupKFold 交叉验证对 Phase 3 产出的特征矩阵进行分类，执行 1000 次置换检验，打印并保存结果。

Purpose: 验证频谱特征对 MDD vs ADHD 的分类能力（AUC > 0.70，置换检验显著）
Output: classifier.py（分类逻辑），main.py（扩展调用），results.json（结果文件）
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-extraction/03-01-SUMMARY.md
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 classifier.py</name>
  <files>classifier.py</files>
  <action>
创建 classifier.py，实现 classify(X, y, groups, n_splits=5, n_permutations=1000, random_state=42) -> dict：

1. LabelEncoder 编码标签（ADHD=0, MDD=1，字母序）；用 list(le.classes_).index("MDD") 获取 MDD 的 proba 列索引
2. Pipeline([("scaler", StandardScaler()), ("svc", SVC(kernel="rbf", probability=True, random_state=random_state))])
3. cv = StratifiedGroupKFold(n_splits=n_splits)
4. proba = cross_val_predict(pipe, X, y_enc, groups=groups, cv=cv, method="predict_proba")
5. pred  = cross_val_predict(pipe, X, y_enc, groups=groups, cv=cv, method="predict")
6. 计算 auc（roc_auc_score），acc（accuracy_score），tn/fp/fn/tp（confusion_matrix ravel()）
7. _, _, pvalue = permutation_test_score(pipe, X, y_enc, groups=groups, cv=cv, n_permutations=n_permutations, scoring="roc_auc", n_jobs=-1, random_state=random_state)
8. 返回 dict，所有数值用 float() 包装：auc, accuracy, sensitivity(tp/(tp+fn)), specificity(tn/(tn+fp)), permutation_pvalue, n_permutations, n_splits

注意：StandardScaler 必须在 Pipeline 内（防数据泄露）；SVC 必须 probability=True。
  </action>
  <verify>python -c "from classifier import classify; print('import ok')"</verify>
  <done>classifier.py 可导入，classify 函数签名正确</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 main.py，打印结果并保存 results.json</name>
  <files>main.py</files>
  <action>
在 main.py 顶部 import 区追加 "import json, datetime"，并在 if __name__ == "__main__": 块末尾（X/y/groups 已构建后）追加：

1. from classifier import classify
2. results = classify(X, y, groups)
3. print(f"AUC={results['auc']:.3f}  ACC={results['accuracy']:.3f}  SEN={results['sensitivity']:.3f}  SPE={results['specificity']:.3f}  p={results['permutation_pvalue']:.4f}")
4. output = {"timestamp": datetime.datetime.now().isoformat(), "condition": CONDITION, "n_subjects": int(X.shape[0]), "n_features": int(X.shape[1]), "label_counts": {k: int(v) for k, v in zip(*np.unique(y, return_counts=True))}, **results}
5. json.dump(output, open("results.json", "w"), indent=2); print("Saved results.json")

classify() 调用必须在 if __name__ == "__main__": 块内（Windows joblib spawn 要求）。
  </action>
  <verify>python -c "import ast; ast.parse(open('main.py').read()); print('syntax ok')"</verify>
  <done>main.py 语法正确，包含 classify() 调用和 json.dump 保存逻辑</done>
</task>

</tasks>

<verification>
运行完整 pipeline：python main.py

预期输出：
- 控制台打印 AUC/ACC/SEN/SPE/p 值一行摘要
- "Saved results.json" 确认文件写入
- results.json 存在且包含 auc, accuracy, sensitivity, specificity, permutation_pvalue 字段
</verification>

<success_criteria>
1. python main.py 完整运行无报错
2. 控制台输出包含 AUC、ACC、SEN、SPE、p 值
3. results.json 存在，json.loads 可解析，包含所有 6 个指标字段
4. AUC 值在合理范围（0.5-1.0）
</success_criteria>

<output>
完成后创建 .planning/phases/04-classification-output/04-01-SUMMARY.md
</output>
