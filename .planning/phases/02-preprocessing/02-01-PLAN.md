---
phase: 02-preprocessing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - preprocessor.py
  - main.py
autonomous: true
requirements: [PREP-01, PREP-02, PREP-03]

must_haves:
  truths:
    - "preprocess(raw) returns clean Epochs with shape (n_epochs, 26, 1000)"
    - "Epochs contain only the 26 scalp EEG channels (non-scalp channels excluded)"
    - "Rejection stats (n_before, n_after) are returned and printed"
    - "main.py runs end-to-end on one subject and prints epoch count + rejection rate"
  artifacts:
    - path: "preprocessor.py"
      provides: "preprocess(raw) -> (Epochs, stats)"
      exports: ["preprocess"]
    - path: "config.py"
      provides: "EEG_CHANNELS list, EPOCH_DURATION, REJECT_THRESHOLD"
      contains: "EEG_CHANNELS"
  key_links:
    - from: "main.py"
      to: "preprocessor.preprocess"
      via: "import and call after load_raw()"
      pattern: "from preprocessor import preprocess"
    - from: "preprocessor.preprocess"
      to: "mne.make_fixed_length_epochs"
      via: "direct call after filter + reref"
      pattern: "make_fixed_length_epochs"
---

<objective>
Implement the EEG preprocessing pipeline: bandpass filter (1-40 Hz), average re-reference, 2s epoching, and amplitude artifact rejection.

Purpose: Transform raw EEG (preload=False) into clean Epochs ready for feature extraction.
Output: preprocessor.py with preprocess() function; config.py updated with preprocessing constants; main.py updated to validate the pipeline on one subject.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preprocessing/02-RESEARCH.md
@.planning/phases/01-data-loading/01-01-SUMMARY.md
@config.py
@data_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: preprocessor.py + config.py preprocessing constants</name>
  <files>preprocessor.py, config.py</files>
  <action>
Append to config.py after existing constants:

    EEG_CHANNELS = [
        'Fp1','Fp2','F7','F3','Fz','F4','F8',
        'FC3','FCz','FC4','T7','C3','Cz','C4','T8',
        'CP3','CPz','CP4','P7','P3','Pz','P4','P8',
        'O1','Oz','O2'
    ]
    EPOCH_DURATION = 2.0       # seconds
    REJECT_THRESHOLD = 100e-6  # peak-to-peak 100 uV (MNE uses PTP, not absolute)

Create preprocessor.py:

    import mne
    from config import EEG_CHANNELS, EPOCH_DURATION, REJECT_THRESHOLD

    def preprocess(raw: mne.io.Raw) -> tuple:
        """Filter, re-reference, epoch, and reject artifacts.
        Returns (Epochs, {'n_before': int, 'n_after': int}).
        Epoch shape: (n_epochs, 26, 1000)
        """
        raw.load_data(verbose=False)
        raw.pick(EEG_CHANNELS)  # exclude non-scalp channels BEFORE re-reference
        raw.filter(1.0, 40.0, fir_design='firwin', verbose=False)
        raw.set_eeg_reference('average', projection=False, verbose=False)
        epochs = mne.make_fixed_length_epochs(raw, duration=EPOCH_DURATION, preload=True, verbose=False)
        n_before = len(epochs)
        epochs.drop_bad(reject=dict(eeg=REJECT_THRESHOLD), verbose=False)
        return epochs, {'n_before': n_before, 'n_after': len(epochs)}

Order is mandatory: pick() -> filter() -> set_eeg_reference() -> epoch -> drop_bad.
Do NOT reorder (re-reference before pick corrupts reference; filter after epoch causes edge artifacts).
  </action>
  <verify>python -c "from preprocessor import preprocess; print('ok')"</verify>
  <done>preprocessor.py imports without error; config.py contains EEG_CHANNELS (26 entries), EPOCH_DURATION, REJECT_THRESHOLD</done>
</task>

<task type="auto">
  <name>Task 2: Update main.py to validate preprocessing on one subject</name>
  <files>main.py</files>
  <action>
Add a Phase 2 validation block to main.py after the existing Phase 1 output. Load the first MDD subject, run preprocess(), print results:

    from preprocessor import preprocess

    # --- Phase 2: Preprocessing validation ---
    subject_id = subjects[0]
    raw = load_raw(subject_id, CONDITION)
    epochs, stats = preprocess(raw)
    print(f"\n--- Preprocessing ---")
    print(f"Subject: {subject_id}")
    print(f"Epochs: {stats['n_after']}/{stats['n_before']} kept "
          f"({100 * (1 - stats['n_after']/stats['n_before']):.1f}% rejected)")
    print(f"Epoch shape: {epochs.get_data().shape}  # (n_epochs, 26, 1000)")
    print(f"Channels: {len(epochs.ch_names)} (expected 26)")

Keep all existing Phase 1 output intact. Only append the Phase 2 block.
  </action>
  <verify>python main.py</verify>
  <done>
    Output includes:
    - "Epochs: N/60 kept (X.X% rejected)" where N is between 50-60
    - "Epoch shape: (N, 26, 1000)"
    - "Channels: 26 (expected 26)"
  </done>
</task>

</tasks>

<verification>
Run: python main.py

Expected output includes existing Phase 1 lines plus:
- "--- Preprocessing ---"
- Epoch shape showing 26 channels and 1000 samples
- Rejection rate ~1-5% (research verified ~1.7% on real data)
</verification>

<success_criteria>
- preprocess(raw) returns Epochs with shape (n_epochs, 26, 1000)
- 26 channels (non-scalp excluded)
- Rejection stats printed
- main.py runs end-to-end without error
</success_criteria>

<output>
After completion, create .planning/phases/02-preprocessing/02-01-SUMMARY.md
</output>
