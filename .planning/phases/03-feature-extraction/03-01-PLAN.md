---
phase: 03-feature-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - feature_extractor.py
  - main.py
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04]

must_haves:
  truths:
    - "每个受试者产出 262 维特征向量（130 绝对功率 + 130 相对功率 + TBR + FAA）"
    - "TBR 仅使用额叶通道（F3/F4/Fz/F7/F8），FAA 使用 ln(alpha_F4) - ln(alpha_F3)"
    - "运行 main.py 后控制台打印 X.shape == (n_subjects, 262)"
    - "空 epochs 的受试者被跳过并记录日志，不导致崩溃"
  artifacts:
    - path: "feature_extractor.py"
      provides: "extract_features(epochs) -> np.ndarray shape (262,)"
      exports: ["extract_features"]
    - path: "config.py"
      provides: "FREQ_BANDS, FRONTAL_CHANNELS constants"
      contains: "FREQ_BANDS"
    - path: "main.py"
      provides: "Subject loop building X, y, groups arrays"
      contains: "extract_features"
  key_links:
    - from: "main.py"
      to: "feature_extractor.extract_features"
      via: "import and call per subject"
      pattern: "extract_features\(epochs\)"
    - from: "feature_extractor.py"
      to: "config.FREQ_BANDS"
      via: "import"
      pattern: "from config import"
---

<objective>
Implement spectral feature extraction: per-subject 262-element feature vector via Welch PSD,
covering absolute band power, relative band power, TBR, and FAA.

Purpose: Produces the feature matrix X needed for Phase 4 classification.
Output: feature_extractor.py, updated config.py and main.py; console prints X.shape.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-extraction/03-RESEARCH.md
@.planning/phases/02-preprocessing/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FREQ_BANDS/FRONTAL_CHANNELS to config.py, create feature_extractor.py</name>
  <files>config.py, feature_extractor.py</files>
  <action>
Append to config.py (after existing constants):

    FREQ_BANDS = {
        "delta": (1, 4), "theta": (4, 8), "alpha": (8, 13),
        "beta": (13, 30), "gamma": (30, 40),
    }
    FRONTAL_CHANNELS = ["F3", "F4", "Fz", "F7", "F8"]

Create feature_extractor.py with extract_features(epochs) -> np.ndarray:
- epochs.compute_psd(method="welch", fmin=1.0, fmax=40.0, n_fft=500, verbose=False)
- psd.get_data(return_freqs=True) -> psds (n_epochs, 26, 40), freqs (40,)
- band_mean(fmin, fmax): mask freqs, mean over freq axis then epoch axis -> (26,)
- abs_bp: {band: band_mean} for each band in FREQ_BANDS  [FEAT-01]
- total = sum(abs_bp.values()); rel_bp = {b: abs_bp[b]/total}  [FEAT-04]
- TBR: per-epoch theta_frontal.mean(ch) / beta_frontal.mean(ch), mean over epochs  [FEAT-02]
  Resolve frontal indices via ch_names.index() for each c in FRONTAL_CHANNELS — NOT hardcoded
- FAA: per-epoch ln(alpha_F4) - ln(alpha_F3), mean over epochs  [FEAT-03]
  Use ch_names.index("F3") and ch_names.index("F4") dynamically
- Return np.concatenate([*[abs_bp[b] for b in FREQ_BANDS],
                         *[rel_bp[b] for b in FREQ_BANDS],
                         [tbr, faa]])  -> shape (262,)

Do NOT use np.trapz (mean over uniform 1 Hz bins is sufficient).
Do NOT hardcode channel indices.
  </action>
  <verify>python -c "import feature_extractor; print('import ok')"</verify>
  <done>feature_extractor.py imports without error; config.py contains FREQ_BANDS and FRONTAL_CHANNELS</done>
</task>

<task type="auto">
  <name>Task 2: Update main.py with full subject feature-extraction loop</name>
  <files>main.py</files>
  <action>
Replace the Phase 2 single-subject validation block with a full subject loop:

    from feature_extractor import extract_features

    features, labels, subject_ids = [], [], []
    for _, row in subjects.iterrows():
        try:
            raw = load_raw(row["participants_ID"], CONDITION)
            epochs, stats = preprocess(raw)
        except Exception as e:
            print(f"[SKIP] {row['participants_ID']}: {e}")
            continue
        if len(epochs) == 0:
            print(f"[SKIP] {row['participants_ID']}: 0 epochs after rejection")
            continue
        features.append(extract_features(epochs))
        labels.append(row["indication"])
        subject_ids.append(row["participants_ID"])

    X = np.array(features)
    y = np.array(labels)
    groups = np.array(subject_ids)
    print(f"Feature matrix: X.shape={X.shape}, y.shape={y.shape}")
    print(f"Labels: {dict(zip(*np.unique(y, return_counts=True)))}")

The `subjects` DataFrame and `CONDITION` are already defined earlier in main.py from Phase 1.
Add `import numpy as np` at top if not already present.
  </action>
  <verify>python D:/eeg/tdbrain/main.py 2>&1 | tail -5</verify>
  <done>Console prints "Feature matrix: X.shape=(N, 262)" where N > 0; no unhandled exceptions</done>
</task>

</tasks>

<verification>
1. python -c "from feature_extractor import extract_features; print('ok')" — no ImportError
2. python D:/eeg/tdbrain/main.py — prints X.shape with second dimension == 262
3. Spot-check: TBR for sub-87999321 EO should be ~5.97 (verified in research)
</verification>

<success_criteria>
- X.shape == (N, 262) where N is number of successfully processed subjects
- No crash on subjects with 0 epochs (skipped with log)
- FAA = ln(alpha_F4) - ln(alpha_F3), not raw difference
- TBR uses only FRONTAL_CHANNELS, not all 26 channels
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-extraction/03-01-SUMMARY.md`
</output>
