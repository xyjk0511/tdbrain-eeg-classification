---
phase: 06-classifier-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - classifier.py
autonomous: true
requirements: [CLF-01, CLF-02, CLF-03, CLF-04, CLF-05]

must_haves:
  truths:
    - classify() returns a dict keyed by model name (svm, rf, xgb)
    - SelectKBest(k=50) is a Pipeline step in every model pipeline, never fit outside CV
    - XGBoost param grid includes max_depth: [2, 3, 4]
    - Permutation test fixed_pipe includes the selector step matching the main pipeline structure
    - config.py exports N_FEATURES_SELECT = 50
  artifacts:
    - path: config.py
      provides: N_FEATURES_SELECT constant
      contains: N_FEATURES_SELECT
    - path: classifier.py
      provides: model registry + multi-model nested CV + per-model results
      exports: [classify]
  key_links:
    - from: classifier.py Pipeline
      to: SelectKBest
      via: Pipeline step named selector
      pattern: selector.*SelectKBest
    - from: classifier.py permutation fixed_pipe
      to: selector step
      via: same Pipeline structure as main pipe
      pattern: fixed_pipe.*selector
---

<objective>
Extend classifier.py with a model registry (SVM, RF, XGBoost) where each model Pipeline includes SelectKBest(k=50) as a step. classify() runs nested CV + permutation test per model and returns a dict of per-model results.

Purpose: Enable multi-model comparison (CLF-01 through CLF-05) without data leakage from feature selection.
Output: Updated config.py (N_FEATURES_SELECT) and classifier.py (model registry, per-model results dict).
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@config.py
@classifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add N_FEATURES_SELECT to config.py</name>
  <files>config.py</files>
  <action>
Append one line to config.py after the existing constants:

    N_FEATURES_SELECT = 50  # SelectKBest k inside Pipeline CV

No other changes.
  </action>
  <verify>python -c "from config import N_FEATURES_SELECT; assert N_FEATURES_SELECT == 50"</verify>
  <done>N_FEATURES_SELECT = 50 importable from config.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite classifier.py with model registry and per-model results</name>
  <files>classifier.py</files>
  <action>
Replace classifier.py. Keep function signature: classify(X, y, groups, n_splits=5, n_permutations=1000, random_state=42) -> dict

Add imports: SelectKBest, f_classif (sklearn.feature_selection); RandomForestClassifier (sklearn.ensemble); XGBClassifier (xgboost); N_FEATURES_SELECT (config).

Inside classify(), build MODEL_REGISTRY as a local dict of (pipe, param_grid) tuples:

  svm: pipe = Pipeline([(scaler, StandardScaler()), (selector, SelectKBest(f_classif, k=N_FEATURES_SELECT)), (clf, SVC(kernel=rbf, probability=True, random_state=rs))])
       grid = {clf__C:[0.1,1,10,100], clf__gamma:[scale,auto], clf__class_weight:[None,balanced]}

  rf:  pipe = Pipeline([(scaler, StandardScaler()), (selector, SelectKBest(f_classif, k=N_FEATURES_SELECT)), (clf, RandomForestClassifier(random_state=rs))])
       grid = {clf__n_estimators:[100,300], clf__max_depth:[None,5,10], clf__class_weight:[None,balanced]}

  xgb: pipe = Pipeline([(scaler, StandardScaler()), (selector, SelectKBest(f_classif, k=N_FEATURES_SELECT)), (clf, XGBClassifier(use_label_encoder=False, eval_metric=logloss, random_state=rs))])
       grid = {clf__n_estimators:[100,300], clf__max_depth:[2,3,4], clf__learning_rate:[0.05,0.1], clf__subsample:[0.8,1.0]}
       NOTE: max_depth=[2,3,4] mandatory -- default 6 overfits on ~300 training samples.

Keep LabelEncoder/mdd_idx logic unchanged.
Initialize once before the loop: outer_cv, inner_cv, rng, unique_groups, group_label_map, group_label_vals, _group_permute (all identical to existing code).

Loop over MODEL_REGISTRY.items(). For each (name, (pipe, param_grid)):

  1. Nested CV (same logic as existing code):
     - outer_cv.split -> GridSearchCV(pipe, param_grid, cv=inner_cv, scoring=roc_auc, refit=True, n_jobs=1)
     - collect probas/preds/test_indices, reconstruct order with argsort
     - compute auc, acc, sensitivity, specificity (same formulas as existing)

  2. fixed_pipe for permutation test -- MUST include selector step:
     svm: Pipeline([(scaler,StandardScaler()), (selector,SelectKBest(f_classif,k=N_FEATURES_SELECT)), (clf,SVC(kernel=rbf,probability=True,class_weight=balanced,random_state=rs))])
     rf:  Pipeline([(scaler,StandardScaler()), (selector,SelectKBest(f_classif,k=N_FEATURES_SELECT)), (clf,RandomForestClassifier(n_estimators=100,class_weight=balanced,random_state=rs))])
     xgb: Pipeline([(scaler,StandardScaler()), (selector,SelectKBest(f_classif,k=N_FEATURES_SELECT)), (clf,XGBClassifier(n_estimators=100,max_depth=3,use_label_encoder=False,eval_metric=logloss,random_state=rs))])
     Run permutation test with same cross_val_score loop as existing code.
     pvalue = (sum(perm_scores >= auc) + 1) / (n_permutations + 1)

  3. results[name] = {auc, accuracy, sensitivity, specificity, permutation_pvalue, n_permutations, n_splits}

Return results dict with keys svm, rf, xgb.
  </action>
  <verify>
cd D:/eeg/tdbrain &amp;&amp; python -c "
import numpy as np
from classifier import classify
rng = np.random.RandomState(0)
X = rng.randn(60, 100)
y = ["MDD"]*30 + ["ADHD"]*30
groups = np.arange(60)
results = classify(X, y, groups, n_splits=3, n_permutations=5)
assert set(results.keys()) == {"svm", "rf", "xgb"}
for k, v in results.items():
    assert "auc" in v and "permutation_pvalue" in v
print("OK", {k: round(v["auc"],3) for k,v in results.items()})
"
  </verify>
  <done>
classify() returns {svm:{...}, rf:{...}, xgb:{...}}.
Each sub-dict has auc, accuracy, sensitivity, specificity, permutation_pvalue, n_permutations, n_splits.
Smoke test passes with 60 samples, 100 features, 3-fold, 5 perms.
  </done>
</task>

</tasks>

<verification>
1. python -c "from config import N_FEATURES_SELECT; assert N_FEATURES_SELECT == 50" passes.
2. Smoke test in Task 2 verify block passes -- three model keys, all required metrics present.
3. grep -n "selector" classifier.py shows selector step in main pipe and fixed_pipe for all three models.
4. grep -n "max_depth" classifier.py shows [2,3,4] in xgb param_grid.
</verification>

<success_criteria>
- classify() returns a 3-key dict (svm, rf, xgb), each with auc/accuracy/sensitivity/specificity/permutation_pvalue.
- SelectKBest(k=50) named selector step inside every Pipeline (main and fixed_pipe).
- XGBoost param_grid has max_depth=[2,3,4].
- No feature selection outside CV.
- Smoke test passes.
</success_criteria>

<output>
After completion, create .planning/phases/06-classifier-registry/06-01-SUMMARY.md
</output>
